// Generate an image tag with responsive images served from cloudinary

// @TODO change to `<img>`, add cloudinary urls, figure out image logic

// @TODO in config add
// config.addShortcode("image", require("./src/components/image.js"))

// module.exports = (id, alt = 'Missing alt text') => {
//   return `<picture>
//   <source srcset="/images/${url}?nf_resize=fit&w=700" media="(min-width: 1200px)">
//   <source srcset="/images/${url}?nf_resize=fit&w=600" media="(min-width: 740px)">
//   <img src="/images/${url}?nf_resize=fit&w=500" alt="${alt}" />
// </picture>`
// }
